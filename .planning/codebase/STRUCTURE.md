# Repository Structure

```
.
├── .github/
│   └── workflows/
│       └── main.yml        # GitHub Action for snake contribution animation
├── .planning/
│   └── codebase/           # GSD codebase mapping documents
└── README.md               # GitHub Profile main page content
```

## Directory Details
- `.github/workflows/main.yml`: Automates building and deploying contribution snake SVG to `output` branch.
- `README.md`: Contains HTML tags (`<h1 align="center">`, `<picture>`, `<img>`) for layout and formatting.
